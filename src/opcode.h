#ifndef FLOP_OPCODE_H
#define FLOP_OPCODE_H

typedef enum Opcode{
    NO_OP,
    PUSH_CONSTANT,
    PUSH_REGISTER,
    PUSH_IO,
    POP_REGISTER,
    POP_IO,
    DUPLICATE,
    DROP,
    COMPARE,
    PEAK_REGISTER,
    PEAK_IO,
    ADD_STACK,
    ADD_REGISTER,
    ADD_CONSTANT,
    SUB_STACK,
    SUB_REGISTER,
    SUB_CONSTANT,
    AND_STACK,
    AND_REGISTER,
    AND_CONSTANT,
    OR_STACK,
    OR_REGISTER,
    OR_CONSTANT,
    XOR_STACK,
    XOR_REGISTER,
    XOR_CONSTANT,
    NOT_STACK,
    NOT_REGISTER,
    SET_FLIP,
    CLEAR_FLIP,
    CLEAR_IO,
    JUMP_ZERO,
    JUMP_NOT_ZERO,
    JUMP_CARRY,
    JUMP_NOT_CARRY,
    JUMP_FLIPPED,
    JUMP_NOT_FLIPPED,
    JUMP_UNCONDITIONAL,
    INPUT,
    OUTPUT_ASCII,
    OUTPUT_NUMBER,
    JUMP_SUBROUTINE,
    RETURN_SUBROUTINE,
    HALT,
};

# endif